# Lane Tools

This directory is reserved for tools built around `lanec`, such as a formatter, REPL, language server, package tooling, or test runners.

Tools may depend on compiler libraries. Compiler core packages should not depend on tools.

