# ISS-008: Render lane run values with source-style type annotations

## Metadata
- Type: bug
- Status: closed
- Priority: 2
- Labels: area/lane-run, area/buslane-interpreter, pretty, debug-output
- Assignee: unassigned
- Created: 2026-06-16
- Updated: 2026-06-16
- External ref: none

## Description
`lane run --entry` currently renders primitive runtime values using internal runtime tags such as:

```text
int 1
```

The user-facing debug output should be source-style and include the selected entry type:

```text
1 : Int
```

The current format exposes implementation representation instead of the language-level value. This matters for the command-line tool because `lane run` is the first user-facing execution path.

## Design
Introduce a `lane run` debug renderer that combines the selected `BuslaneEntry` type with the runtime value. `BuslaneEntry` already stores `type_`, so the renderer should not rely on `BuslaneRuntimeValue` alone when producing command output.

For primitives, render source-style values followed by ` : Type`. Function entries may continue to render as an opaque callable such as `<function> : (T1, ..., Tn) -> R` if the type is available. Nominal data rendering should also move toward source-style output, but the first required fix is primitive values.

This issue is about user-facing output, not evaluation semantics.

## Acceptance Criteria
- [x] `lane run` renders an integer entry as `1 : Int`, not `int 1`.
- [x] Boolean, string, and unit entries render with source-style values and type annotations.
- [x] Function entries include their function type or otherwise use a documented finite callable rendering.
- [x] The implementation uses the selected entry's Buslane type when rendering the output.
- [x] Regression tests cover at least an integer entry through `lane run` or the command-layer rendering helper.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-005, ISS-007
- Discovered from: none

## Notes
- 2026-06-16: Discovered after manual `lane run --entry` smoke testing printed `int 42`; desired direction is `42 : Int`.

## Close Notes
- 2026-06-16: Closed by adding `BuslaneRuntimeValue::pretty_with_type` and routing `lane run` output through the selected `BuslaneEntry.type_`. Primitive entries now render as source-style values with type annotations, and function entries render as finite callable values with their function type. Added lane command-layer tests for integer, function, bool, string, and unit rendering. Verified with `moon test lane-tools/lane --warn-list +73` and manual `lane run` checks for `01_top_level_forward_function_value.lane`, `05_top_level_contextual_offer.lane`, and `11_function_types.lane`.
